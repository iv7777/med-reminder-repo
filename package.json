{
  "name": "med-reminder",
  "version": "1.0.0",
  "description": "Family medication reminder — Cloudflare Pages + D1",
  "scripts": {
    "dev": "wrangler pages dev public --d1=DB",
    "deploy": "wrangler pages deploy public --project-name=med-reminder",
    "db:schema": "wrangler d1 execute med-reminder --file=schema.sql",
    "db:seed": "wrangler d1 execute med-reminder --file=seed.sql",
    "db:reset": "wrangler d1 execute med-reminder --file=schema.sql && wrangler d1 execute med-reminder --file=seed.sql"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
